# T01 — Tauri 셸: 기존 UI를 데스크톱 앱으로
Time: started=2026-08-25T07:35:44+09:00 finished=2026-08-25T07:32:04+09:00

## Goal
기존 React+Vite/Hono 자산을 macOS Tauri 셸로 감싸 데스크톱 앱으로 실행한다.

## Why this slice
D1 본체 - 이후 모든 티켓의 실행·검증 기반이 되는 첫 수직 조각.

## Scope
- `src-tauri/` 신설(창 구성, 프론트엔드 로드), tauri dev/build 스크립트 연동, cargo check/clippy 게이트
- 기존 scripts/dev 경로는 유지(웹 실행 대안)

## Implementation notes
- UI 코드 변경 금지 - 셸 계층만.
- 백엔드(Hono)는 Tauri sidecar 또는 앱 내 스레드로 구동 - 창이 닫히면 정리.

## Acceptance criteria
spec 수용기준 1번: `tauri dev`로 창 오픈 + 현재 UI 전 기능 동작; cargo check/clippy green; 기존 vitest green.

## Verification
수동 시나리오 + cargo/vitest.

## Depends on
- none

## Can run in parallel with
- nothing (T02+가 이 셸 위에서 진행)
