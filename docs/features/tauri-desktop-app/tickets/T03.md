# T03 — FS 이벤트 실시간 감시 (+폴링 폴백)

## Goal
지정 루트와 firstmate 홈 백로그 파일의 변화를 이벤트로 감지해 수 초 내 화면에 반영한다.

## Why this slice
INV-3(stale 금지)의 실시간성을 폴링 주기 없이 달성하는 D3 본체.

## Scope
- `src-tauri/`: notify 기반 FS 감시 명령(루트 재귀, debounce), 이벤트→프론트엔드 emit; 감시 불가 시 폴링 간격 폴백 신호
- frontend/core: 이벤트 수신 → 해당 프로젝트 리-read 트리거(기존 결정적 파서 재호출), 폴백 폴러

## Implementation notes
- 판정은 여전히 결정적(INV-4): 이벤트는 "다시 읽어라" 신호일 뿐, 해석은 기존 리더가 한다.
- 감시 대상: docs/features/**, 백로그 파일.

## Acceptance criteria
파일 추가/변경 → 수 초 내 반영(신규 feature/ticket 포함); 감시 실패 시 폴백 동작; CPU 안정.

## Verification
수동(파일 생성/수정/삭제 시나리오) + 감시 debouncer 단위 테스트.

## Depends on
- T02 (감시 루트/백로그 경로 설정값)

## Can run in parallel with
- nothing

## Comments

**(2026-08-25 구현 착지) 감시기 위치가 본문 Scope 과 다르다 — src-tauri 가 아니라 백엔드 chokidar seam.**
본문 Scope 의 "`src-tauri/`: notify 기반 FS 감시 명령"(위 spec §Scope 의 같은 줄도)은 그대로 이행되지
않았고, firstmate 승인된 의도 변경이다 — 코드를 티켓과 어긋난 것으로 읽고 되돌리지 말 것. 근거:
T01 이 착지한 셸 구조(UI 는 순수 http+WS 단일 전송로, IPC 는 폴더 다이얼로그 하나, 셸은 수명 관리만)에서
Rust 에 감시기를 새로 두면 설정 파싱과 이벤트 전송로가 이중화된다. D3(FS 이벤트+주기 풀스캔 백업,
결정적 파서 재사용)의 요건은 이미 백엔드 chokidar seam 이 갖추고 있어 그 위에 확장했다 — 웹 실행 경로와
같은 생산자·같은 소비처(INV-4 단일 릴레이 유지). 실제 착지: `core-io/src/watch.ts`·
`core-io/src/backlog-watch.ts`(감시 원천), `backend/src/watchers.ts`(셋을 묶어 소스별 폴백 판정),
contract `ChangeEvent` 의 `backlog`·`watch-fallback`(기존 WS 전송로로 흘린다). 자세한 근거는 커밋
ce2f93f 메시지.
