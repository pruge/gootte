# T07 — 통합 테스트 + gootte 이관 실행 + 실물 확인

**Blocked by:** T03, T04, T05, T06

**Time:** started=2026-09-09T10:06:30+09:00

## Goal

state.json 모드의 종단 테스트를 채우고, gootte 자신에 이관을 실행해
CLI·백엔드·배지가 같은 값을 보는지 실물로 확인한다.

## Why this slice

컴파일·단위만으로는 캡틴 화면의 배지 즉시성·elapsed 툴팁·CLI 일치를 보증할 수
없다(verify gate — 컴파일만으로 완료 금지).

## Produces

- `scripts/tests/gootte-time.test.sh` state.json 모드 케이스
  (TS 위임·worktree→메인·cancel·drop 배치)
- gootte 자신의 state.json v2 + 144장 이관
- 실물 확인 기록(CLI status vs 백엔드 응답 대조)

## Consumers

- T08 문서 — 실물 확인 결과가 문서 갱신의 근거

## Touched surfaces

- `scripts/tests/gootte-time.test.sh` — 케이스 추가
- `bin/gootte` — 필요 시 위임 경로 수정

## Explicitly out of scope

- MD 줄 삭제 — 하지 않는다
- 커밋 — 캡틴 명시 요청 시만

## Locked decisions

- 이관 실행은 **gootte 자신에게만**(캡틴 결정 1)

## Evidence anchors

- `scripts/tests/gootte-time.test.sh` — 기존 20 케이스의 임시 디렉토리 픽스처 관례

## Regression guards

- 🔴 MD 모드 기존 20 케이스 유지
- 🔴 `pnpm verify` 전체 green
- 🔴 실물: `gootte start` → `gootte status`·백엔드 `/api/features/gootte`·state.json 셋이 같다

## Scope

1. state.json 모드 케이스 추가(TS 위임 경로)
2. `gootte migrate-time --dry-run` → 실실행 → 대조 리포트 확인
3. 실물 확인: CLI status / backend /api/features/:slug / state.json 대조
4. 배지 즉시성 확인(기록 후 /api/projects)

## Implementation notes

- gootte에 `gootte start` 실행 시 `GOOTTE_DATA_DIR` 격리 규율 준수
- 백엔드 실물 확인은 dev 서버 스킬(gootte-dev-server) 규율을 따른다

## Acceptance criteria

- AC1 — gootte-time.test.sh 전체 통과(기존+신규)
- AC2 — 이관 후 대조 차이 0
- AC3 — 실물 셋 일치
- AC4 — `pnpm verify` green

## Verification

`pnpm verify` + 실물 확인
