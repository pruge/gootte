# T06 — 캡틴 검수 (메모리·백로그 그래프 종착)

**Blocked by:** T01, T02, T03, T04, T05 — 그래프 전체가 끝나야 열린다

## Goal

캡틴이 landed 결과를 한 번에 보고 승인한다. 가벼운 피드백은 이 세션에서 바로 고치고 문서에 반영한다.

## Why this slice

task-planning 종착 규칙 — 승인 없이 그래프를 닫지 않는다.

## Produces

- 캡틴의 승인(또는 추가 작업 목록).

## Consumers

- 없음.

## Touched surfaces

- 없음. 피드백이 있으면 해당 티켓 파일을 고친다.

## Explicitly out of scope

- 새 기능·추가 최적화 — 나오면 새 부모 계획으로(recursion gate).

## Locked decisions

- D1~D6(spec 참조). 계획 승인은 구현 승인이며 커밋·푸시 승인이 아니다(명시 요청 시만).

## Evidence anchors

- `docs/features/memory-diet-drop-backlog/spec.md` 및 T01~T05 의 landed diff.
- 변경 전후 RSS 측정 기록(T01~T05 Comments).

## Regression guards

- `pnpm verify` green(검수 시점 재확인).

## Scope

- 검수 + 가벼운 피드백 반영. 큰 피드백은 새 계획으로.

## Implementation notes

- 검수 항목: A1(RSS 감소) · A2(백로그 무신호) · A3(동일 응답) · A4(홈·discover 정상) · A5(verify green).

## Acceptance criteria

- 캡틴 승인.

## Verification

- `pnpm verify` 재확인.

## Comments
