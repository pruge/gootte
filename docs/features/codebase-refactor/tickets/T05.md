# T05 — stale 주석 정리 + 최종 검증

**Blocked by:** T01, T02, T03, T04

**Time:** started=2026-09-08T05:21:47+09:00 finished=2026-09-08T05:30:47+09:00

## Goal

삭제된 기능을 참조하는 오래된 주석을 정리하고, 전체 리팩토링의 최종 검증을 수행한다.

## Why this slice

stale 주석은 독자를 혼란스럽게 한다. 마지막 단계에서 정리하면 이전 티켓들의 변경이 모두 반영된 상태에서 마무리할 수 있다.

## Produces

- stale 주석 3곳 정리
- dead import 1건 제거
- 전체 리팩토링의 최종 검증 완료

## Consumers

주석은 코드 동작에 영향을 없으나, 독자(개발자)의 이해에 영향을 준다.

## Touched surfaces

| 파일 | 변경 |
|---|---|
| `contract/src/index.ts:75` | 삭제된 `computeMismatches` 참조 주석 정리 |
| `core-io/src/watch.ts:84` | 삭제된 `cling` / `scanWorktrees` 참조 주석 정리 |
| `discover-cache.ts:52` | `unlanded` 관련 오래된 주석 정리 |

## Explicitly out of scope

- 코드 동작 변경 없음
- 새 기능 추가 없음

## Locked decisions

- 이 티켓은 모든 이전 티켓이 완료된 후에만 실행
- 주석 정리만 수행, 코드 변경 없음

## Evidence anchors

- `contract/src/index.ts:75` — `computeMismatches` 참조
- `core-io/src/watch.ts:84` — `cling` / `scanWorktrees` 참조
- `discover-cache.ts` — `unlanded` 관련 주석

## Regression guards

- `pnpm verify` (최종 전체 회귀)
- 이전 티켓(T01-T04)의 모든 변경이 올바르게 유지되는지 확인

## Scope

1단계: stale 주석 3곳 정리
2단계: `pnpm verify` 최종 실행
3단계: 전체 변경 사항 검토 (git diff)

## Implementation notes

- 주석 정리 시 원본 문맥을 파악하고, 해당 주석이 여전히 유효한지 판단
- `computeMismatches` 주석은 plan-board/01에서 제거됨을 설명하는 것이므로, 주석 자체를 삭제하거나 "이전에 존재했던 기능"임을 명시
- `cling` 주석은 git history에서 삭제된 시점을 파악하여 보존할 가치가 있는지 판단

## Acceptance criteria

- stale 주석 3곳 정리됨
- `pnpm verify` 최종 통과
- git diff에서 예상된 변경만 존재 (주석 정리 + 이전 티켓들의 변경)

## Verification

`pnpm verify` + `git diff` 검토
