# T03 — `SettingsDialog` 제거 + 테스트 이전
Time: started=2026-08-30T18:51:07+09:00 finished=2026-08-30T18:53:20+09:00

## Goal

T01·T02 가 완료된 후, 옛 `SettingsDialog.tsx` 파일과 `settings-dialog.test.tsx` 를
제거한다. `SettingsView`(T02)가 모든 기능을 대체했으므로, 소스에 남아 있으면 혼란을 준다.
테스트는 `settings-view.test.tsx`(T02)로 이전·확장되었으므로, 옛 파일을 지우는 것만으로
충분하다.

## Why this slice

T01·T02 가 병존 기간(둘 다 동시에 존재)을 허용했으므로, 이 티켓은 그 병존을 끝내는
정리 작업이다. 코드 크기·렌더 경로·혼란을 줄인다.

## Scope

- `SettingsDialog.tsx` 삭제
- `settings-dialog.test.tsx` 삭제
- `MainPanel.tsx` 에서 `SettingsDialog` import 제거(T01 에서 이미 `SettingsView` 로 교체)

## Touched surfaces

- `code/web/frontend/src/components/settings/SettingsDialog.tsx` — 삭제
- `code/web/frontend/test/settings-dialog.test.tsx` — 삭제
- `code/web/frontend/src/components/main/MainPanel.tsx` — import 문 정리

## Regression guards

- `pnpm -C code/web frontend verify` — `settings-view.test.tsx` 가 기존 커버리지를 대체했음을
  확인. `SettingsDialog` 관련 전역 grep 에 결과 없음 확인.

## Acceptance criteria

1. `SettingsDialog.tsx` 파일이 디스크에 없다.
2. `settings-dialog.test.tsx` 파일이 디스크에 없다.
3. `MainPanel.tsx` 에 `SettingsDialog` import 가 없다.
4. `pnpm -C code/web frontend verify` green.

## Verification

`pnpm verify` — 루트 전체 회귀. `grep -r "SettingsDialog" code/web/frontend/src/` 결과 없음.

## Depends on

- T01 (셸 배치)
- T02 (설정 폼 레이아웃)

## Can run in parallel with

- none
