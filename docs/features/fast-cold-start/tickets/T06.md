# T06 — Captain review (terminal ticket)

## Goal
Captain reviews the completed work and sign-off on whether the caching and performance fixes are acceptable. The feature “project list loads instantly after first load” is complete.

## Why this slice
All preceding tickets (T01–T05) address performance and caching. The captain needs to review the aggregate result and decide whether to accept the implementation or request changes.

## Why this slice
The captain is the final arbiter of whether the caching fix (T01–T05) is acceptable. The tickets address:
- Cached `/api/projects` payload (T01)
- Fallback polling exclusion of `projects` (T02)
- Feature tab caching (T03)
- Plan/steps tab caching (T04)
- Git subprocess reduction (T05)

## Acceptance criteria
1. `GET /api/projects` first call: ~13s (git subprocesses). Subsequent calls within 5s: <100ms (cache hit).
2. Fallback polling no longer hammer `/api/projects` every 5s (excludes `projects` key).
3. Doc change via watcher clears cache → next request recomputes.
4. `pnpm -C code/web verify` green (921 tests pass, or updated count).
5. Spinner no longer spins “forever” — first load shows 13s, then instant.

## Verification
- `curl -s http://localhost:8804/api/projects` first call: 12-15s.
- Second call within 5s: <100ms (cached).
- `curl -s http://localhost:8804/api/projects` after doc change: recompute (stale at 5s).
- `pnpm -C code/web verify` green.
- Manual: open the sidebar, note spinner duration, refresh, note it’s instant.

---

**Ticket close criteria**:
- Captain sign-off: “the feature list loads instantly after first load, and the spinner no longer spins repeatedly.”
- Or: “requested changes — adjust caching invalidation / git subprocess count.”

---

**Captain sign-off**:
_________________________
(날짜)

---

**Related tickets**: T01–T05.

---

## Captain sign-off

_________________________
(날짜)

---

**Related tickets**: T01–T05.