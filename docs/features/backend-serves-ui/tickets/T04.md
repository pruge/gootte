# T04 — 캡틴 검수 (backend 서빙 종착)

**Blocked by:** T01, T02, T03 — landed 결과를 보고 승인한다

## Goal

캡틴이 landed 결과를 보고 승인한다.

## Why this slice

task-planning 종착 규칙 — 승인 없이 그래프를 닫지 않는다.

## Produces

- 캡틴의 승인(또는 추가 작업 목록).

## Consumers

- 없음.

## Touched surfaces

- 없음. 피드백이 있으면 해당 티켓 파일을 고친다.

## Explicitly out of scope

- 새 기능·추가 최적화 — 나오면 새 부모 계획으로.

## Locked decisions

- D1~D8(spec 참조). 계획 승인은 구현 승인이며 커밋·푸시 승인이 아니다.

## Evidence anchors

- `docs/features/backend-serves-ui/spec.md` 및 T01~T03 landed diff.
- 프로세스 목록(A1) · `/`·`/api`·WS 확인(A2).

## Regression guards

- `pnpm verify` + `cargo check`/`clippy` green(검수 시점 재확인).

## Scope

- 검수 + 가벼운 피드백 반영. 큰 피드백은 새 계획으로.

## Implementation notes

- 검수 항목: A1(vite 없음) · A2(화면·API·실시간) · A3(dist 없음 실패) · A4(dev·e2e 무영향) · A5(green).

## Acceptance criteria

- 캡틴 승인.

## Verification

- gate 재확인.

## Comments
