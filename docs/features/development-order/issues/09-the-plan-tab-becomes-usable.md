# 09 — `plan` 탭을 캡틴이 실제로 쓸 수 있게 만든다

**What to build:** 캡틴이 `plan` 탭을 쓰시면서 부딪힌 것 넷을 **한 번에** 고친다 —
쓸모없는 상자를 걷어내고, 남은 상자들이 서로 구분되게 하고, 단계 보기를 기능 보기처럼
세로 칸으로 바꾸고, 조작 줄이 스크롤해도 화면에 남게 한다.

**Blocked by:** 없음 — 지금 착수 가능

**Status:** ready-for-agent

## 🔴 왜 한 티켓인가

넷 다 **같은 화면(`plan` 탭)**, 같은 사람, 그리고 **같은 캡틴 확인 한 번**이다.
쪼개면 파이프라인을 네 번 돌리고 캡틴을 네 번 부른다 — 그런데 캡틴은 한 번에 한 화면만
확인하실 수 있으므로 **나머지 셋은 작업자가 사본만 잡고 기다린다.**

그리고 **서로 얽혀 있다.** ①이 지울 상자를 ②가 다듬으면 일이 두 번 된다.
🔴 **①을 먼저 하고 나머지를 그 위에서 한다.**

| | 무엇 | 캡틴 지시 (2026-08-11, 원문) |
|---|---|---|
| ① | 의견 물어보기를 **걷어낸다** | "저게 맨 위에 있으면 정작 봐야할 내용이 뒤로 밀리잖아. **아예 없애.**" |
| ② | 주황색 상자 둘이 **구분되고**, 경고가 스스로 사라진다 | "어긋나는 것이 없으면 주황색 box가 안보였었는데. 지금은 없는데도 남아 있네." |
| ③ | 단계 보기를 **기능 보기처럼 세로 칸**으로 | "단계 보기도 기능 보기 처럼 해. 3column으로 배치하고 그안에 ticket들 놓는것 처럼." |
| ④ | 보기 전환과 `next` 가 **스크롤해도 남는다** | "단계보기 기능보기 있는 부분이 스크롤시 화면 밖으로 넘어가는데. fix시켜." |

---

## ① 의견 물어보기를 걷어낸다

### 🔴 요청을 거꾸로 만들었다

티켓 06 의 출발점은 캡틴의 이 말이었다:

> "그때는 **네가 자동으로 인지하여 의견을 말했으면** 한다.
> background에서 넘겨주는 방식도 괜찮고. 아니면 버튼을 두어 네게 알리도록해도 좋고."

**planner 가 알아채고 planner 가 말한다** — 그것이 요청이었고, 버튼은 나르는 방법 중 하나였다.
만들어진 것은 반대다:

| 캡틴이 요청하신 것 | 만들어진 것 |
|---|---|
| planner 가 알아채서 **말해 준다** | 캡틴이 상자를 발견하고 **누르러 가신다** |
| 캡틴은 아무것도 안 하신다 | 누를지 판단하고, 기다리고, 화면으로 **돌아와 확인**하신다 |

그리고 캡틴과 planner 는 **이미 대화 창에서 이야기한다.** 거기서는 맥락 전부를 갖고 몇 초 만에
답이 오간다. 이 화면은 그것의 **느리고 얇은 판본**이었고, 그 대가로 **계획이 아래로 밀렸다.**
🔴 **더 나은 통로가 이미 있는데 못한 통로를 화면 맨 위에 세우면 안 된다.**

### 지울 것

| 어디 | 무엇 |
|---|---|
| 화면 | 의견 요청 상자(패널 컴포넌트)와 그것을 부르는 자리 |
| 서버 | 의견 요청을 받고 내주는 경로 |
| 저장 | `opinion_request` 표와 그 저장 코드 |
| 계산 | 버튼이 뜰 조건을 판단하는 순수 함수 |
| 터미널 | `ask` · `ask show` · `ask answer` 와 사용법 줄 |
| 타입·테스트 | 위에만 쓰이던 것들 |

🔴 **상자만 지우고 계산을 남기지 마라.** 아무도 안 쓰는 코드와 아무도 안 읽는 표가 남으면
다음 사람이 그것을 되살릴 근거로 삼는다.

🟢 되살리기는 언제든 싸다 — 이 절과 06 이 이유를 갖고 있다. 필요해지면 그때
**planner 가 먼저 말을 거는 모양**으로 새로 쓴다. 버튼으로 되돌리지 않는다.

🔴 **06 티켓 파일은 지우지 마라.** 착지한 기록이고, 왜 되돌렸는지가 거기 있어야 한다.
맨 위에 09 가 걷어냈다는 줄만 붙인다.

---

## ② 주황색 상자 하나가 서로 다른 두 가지를 말한다

### 🔴 캡틴이 보신 것은 어긋남 상자가 아니었다

그때 어긋남은 실제로 0 이었다 — 살아 있는 서버에 물어 확인했다:

| 확인한 것 | 결과 |
|---|---|
| `gootte order jinwooauto` | `어긋남: 없음` |
| 떠 있는 서버 `/api/plan/jinwooauto` | `mismatches: 0`, 빈 배열 |
| 어긋남 목록 컴포넌트 | 0 건이면 아무것도 안 그린다 — **정상 동작** |

남아 있던 것은 **드래그 경고 상자**다. 🔴 **둘이 같은 옷을 입고 나란히 서 있다.**

| | 어긋남 | 드래그 경고 |
|---|---|---|
| 테두리·배경·모서리·글자색 | `partial` 계열 | **똑같다** |
| 자리 | `plan` 탭 위쪽 | **바로 붙어 있다** |
| 뜻 | 계획과 티켓이 갈라졌다 | 방금 그 드래그가 뭔가에 걸렸다 |
| 언제 사라지나 | 어긋남이 없어지면 자동 | 🔴 **캡틴이 ✕ 를 누를 때만** |

**캡틴이 하나를 다른 하나로 읽으신 것은 착각이 아니라 화면의 결함이다** —
구분 불가능하게 생겼으면 구분해서 보라고 요구할 수 없다.

🔴 **이 결함은 06 이 이미 예고했다**(*"04 의 즉시 검사와 섞지 마라 … 화면에서도 두 덩이가
구분돼야 한다"*). 그 규칙이 06 에만 적용되고 **02 의 어긋남과 04 의 경고 사이에는 안 적용됐다.**

### 고칠 것 둘

**경고가 스스로 없어진다.** 지금은 드롭 성공 시 세팅되고 ✕ 로만 지워진다. 그래서
캡틴이 곧바로 다시 끌어 문제를 없애도, 계획이 다른 경로(터미널 · 티켓 08 의 자동 제거)로
바뀌어도 그대로 남는다. 🔴 **경고는 그 시점의 배치에 대한 말이므로, 배치가 바뀌면 더 이상
참이 아니다.** 다시 성립하지 않는 경고는 스스로 없어져야 한다.

🔴 **판정을 화면에서 새로 짜지 마라.** 네 검사는 이미 서버(`checkTicketDragWarnings`)가
계산한다 — 화면이 다시 판정하면 그 순간 두 답이 생기고 하나는 거짓이다. **다시 물어서 갱신한다.**

🟡 **✕ 는 남긴다.** 캡틴이 알면서 그대로 두시는 것이 정당할 수 있고(티켓 04),
닫은 것은 배치가 다시 바뀌기 전까지 닫힌 채로 있어야 한다.

**둘이 서로 다르게 생긴다.** 🔴 **색만 바꿔 때우지 마라.** 차이는 색이 아니라 성격이다:

| | 어긋남 | 드래그 경고 |
|---|---|---|
| 무엇에 대한 말인가 | **계획 전체**의 상태 | **방금 한 동작** 하나 |
| 언제 생기나 | 계획이나 티켓이 바뀔 때 | 캡틴이 놓는 순간 |
| 놓칠 때의 대가 | 계획이 조용히 낡는다 | 이번 드래그가 조금 어긋난다 |

**항상 있는 것**과 **방금 일어난 것**이 같은 무게로 보이면 안 된다.
모양은 구현자가 정하되, **위 성격 차이가 읽히는지**가 판정 기준이다.
🔴 **어긋남을 접거나 숨기지 마라**(티켓 03) — 그 규칙은 안 건드린다.

---

## ③ 단계 보기를 기능 보기처럼 — 트랙이 세로 칸이 된다

### 캡틴 지시 (2026-08-11, 원문)

> "단계에서 track의 영역을 ticket이 침범하지 못하게해."

> "**단계 보기도 기능 보기 처럼 해. 3column으로 배치하고 그안에 ticket들 놓는것 처럼.**"

### 🔴 두 번째 지시가 첫 번째를 흡수한다

지금 단계 보기는 **단계가 가로줄**이고 줄 안에서 트랙이 왼쪽 이름 칸으로 갈린다.
그 이름 칸을 티켓이 침범하는 것이 캡틴이 처음 지적하신 것이다.

원인은 이렇다 — 바깥 줄에도 줄바꿈이 걸려 있어, 칩 묶음이 남은 폭에 안 들어가는 순간
묶음 **통째로** 다음 줄로 내려가고 그 줄은 **맨 왼쪽**, 곧 트랙 이름 자리에서 시작한다.

🟢 **트랙을 세로 칸으로 바꾸면 그 이름 칸이 아예 사라진다** — 침범할 자리가 없어진다.
그래서 ③은 배치 수리가 아니라 **단계 보기를 기능 보기와 같은 꼴로 바꾸는 일**이다.

### 무엇으로 바꾸나

기능 보기와 **같은 얼개**를 쓴다 — 트랙마다 세로 칸, 넓으면 최대 세 칸,
좁아지면 두 칸·한 칸으로 접힌다. 칸 안에는 **단계별로 티켓**이 놓인다.

```
   웹·권한          낱개            게이트웨이 제어
 ┌───────────┐  ┌───────────┐  ┌───────────┐
 │ 단계 1    │  │ 단계 1    │  │ 단계 1    │   ← 같은 높이에 선다
 │  칩 칩    │  │  칩 칩 칩 │  │  칩       │
 │ 단계 2    │  │           │  │ 단계 2    │
 │  칩       │  │           │  │  칩 칩    │
 └───────────┘  └───────────┘  └───────────┘
```

🔴 **같은 단계가 세 칸에서 같은 높이에 서야 한다.** 단계 보기가 존재하는 이유가
*"같은 단계 = 병렬"* 인데, 칸마다 단계가 제멋대로 흐르면 **그 하나가 화면에서 사라진다.**
어느 트랙에 그 단계가 비어 있어도 자리는 유지된다 — 비었다는 것도 정보다.

- 🔴 **칩이 자기 칸을 안 넘는다.** 긴 제목은 잘리고 가로로 흘러넘치지 않는다.
- 🔴 **트랙을 한 줄로 펴지 마라**(티켓 03 금지 조항) — 이 변경은 그 규칙을 더 잘 지킨다.
- 칸이 접혀 한 칸이 되어도 **어느 티켓이 어느 트랙인지** 알 수 있어야 한다.

### 드래그는 그대로 동작한다

- 칩을 **같은 칸의 다른 단계**로 끌면 → 단계가 바뀐다.
- 칩을 **단계와 단계 사이**로 끌면 → 새 단계가 생긴다(티켓 04).
- 🟡 칩을 **다른 칸**으로 끌면 → 그 티켓이 속한 **기능의 트랙**이 바뀐다.
  기능 보기에서 카드를 다른 칸으로 끄는 것과 같은 뜻이다.
  🔴 **트랙은 기능이 갖는다** — 티켓 하나만 다른 트랙으로 보낼 수는 없다.
  그 사실이 화면에서 읽혀야 하고, 읽히지 않으면 그 조작은 **막는 대신 무슨 일이 일어나는지
  먼저 알려라.**

🔴 **놓을 자리가 끄는 동안 보여야 한다**(티켓 04 캡틴 확인 1). 칸이 바뀌면
그 표시도 새 얼개에 맞게 다시 서야 한다.

### 🟢 기능 보기는 안 바꾼다

이미 캡틴이 원하시는 꼴이다. **그 얼개를 재료로 쓰되, 기능 보기의 동작은 하나도 안 바꾼다.**
두 보기가 같은 얼개를 쓰게 되므로 🔴 **같은 것을 두 벌 짓지 마라** — 칸 나누는 부분은 함께 쓴다.

---

## ④ 보기 전환과 `next` 가 스크롤해도 남는다

### 원인

`plan` 탭이 **통째로 하나의 스크롤 영역**이고 조작 줄이 **그 안의 첫 자식**이다.
계획을 훑어 내려가면 조작 줄도 같이 밀려 올라간다.

**왜 아픈가:** 계획이 길수록 아래에서 보내는 시간이 긴데, **정작 거기서 보기를 바꾸거나
`next` 를 누르고 싶어진다.** 맨 위까지 올라갔다 누르고 다시 내려와 보던 자리를 찾아야 한다.
🔴 **목록이 길수록 나빠지는 종류의 결함**이다.

### 무엇을 바꾸나

🔴 **조작 줄은 스크롤 대상이 아니다.** 계획만 스크롤하고 조작 줄은 제자리에 있는다.
단계·기능 보기 전환, `next` 버튼, "되돌리기 없음" 표시가 함께 남는다.

- 🔴 **가로 스크롤을 만들지 마라.** 좁은 창에서 접히더라도 옆으로 밀려나면 안 된다.
- 🔴 **본문을 가리지 마라.** 붙어 있는 줄 아래로 첫 단계가 반쯤 숨으면 안 된다.
- 🟡 밑을 지나가는 계획이 **비쳐 보이면 안 된다** — 붙박이가 아니라 고장으로 읽힌다.
- 🟡 조작 줄이 **지금보다 두꺼워지지 않게** 한다. 계획 볼 자리가 줄어든다.

🟢 **`features` 탭도 같은 얼개인지 보고, 같으면 같이 고친다** — 두 탭이 다르게 움직이면
그 차이 자체가 다음 질문거리가 된다.

---

## 🔴 넷 모두에 걸리는 금지

- **`extra` 를 건드리지 마라.** `ask` 와 `extra` 는 firstmate 를 깨우는 같은 규약을 쓰지만
  다른 물건이다. `ask` 가 사라져도 **`extra` 는 그대로 동작해야** 하고 firstmate 가 여전히 깨어난다.
- **드래그의 네 검사(티켓 04)를 없애지 마라.** 캡틴이 지목하신 것은 의견 요청 상자뿐이다.
- **`next` · 어긋남 · 단계 · 순위 계산 결과가 하나도 바뀌면 안 된다.**
  ①은 빼기만, ②③④는 배치와 수명만 고친다.
- **트랙을 한 줄로 펴지 마라**(티켓 03 금지 조항).
- **`features` 탭의 동작을 바꾸지 마라** — ③④의 "같으면 같이 고친다"는 **같은 결함의 수리**이지
  개편이 아니다.

## 완료 시 시연 가능한 것

캡틴이 `plan` 탭을 연다. **맨 위에 의견 요청 상자가 없고**, 순서를 아무리 끌어 바꿔도 안 나타난다.
어긋남도 경고도 없을 때 **주황색 상자가 하나도 없다.** 티켓 칩을 막힘 역행이 되게 끌면 경고가
뜨고, 곧바로 되돌리면 **스스로 사라진다.** 일부러 어긋남을 만들면 그 상자는 경고와 **한눈에
다르게** 생겼다. 단계 보기는 **트랙마다 세로 칸**이고 같은 단계가 칸들 사이에서 **나란히** 선다.
계획을 맨 아래까지 내려도 **보기 전환과 `next` 가 화면에 그대로 있어** 거기서 바로 누를 수 있다.
`gootte ask` 는 없는 명령이고, `gootte extra` 는 그대로 동작한다.

## 완료 조건

**①**
- [ ] 🔴 `plan` 탭 어디에도 의견 요청 상자·버튼이 **없다**
- [ ] 서버 경로 · 저장 표 · 판단 계산 · 터미널 명령 · 전용 타입 · 전용 테스트가 전부 사라졌다
- [ ] 이미 저장된 의견 요청이 있어도 불러오다 **깨지지 않는다**
- [ ] 06 티켓 파일이 남아 있고 맨 위에 걷어냈다는 표시가 있다

**②**
- [ ] 어긋남 0 · 유효 경고 0 이면 주황색 상자가 **하나도 없다**
- [ ] 드래그 경고가 조건이 사라지면 **✕ 없이 사라진다**
- [ ] 계획이 다른 경로로 바뀌어도 낡은 경고가 안 남는다
- [ ] 🔴 경고 판정을 **화면에서 다시 짜지 않는다**
- [ ] ✕ 로 닫은 경고는 배치가 다시 바뀌기 전까지 닫혀 있다
- [ ] 어긋남 상자와 경고 상자가 **한눈에 구분된다**
- [ ] 🔴 어긋남은 여전히 접히지도 숨겨지지도 않는다

**③**
- [ ] 🔴 단계 보기가 **트랙마다 세로 칸**이고, 넓으면 세 칸까지 선다
- [ ] 🔴 **같은 단계가 칸들 사이에서 같은 높이**에 선다 — 비어 있는 트랙도 자리를 지킨다
- [ ] 칸 안에서 단계가 위에서 아래로 순서대로 놓인다
- [ ] 긴 제목이 잘리고 칩이 **자기 칸을 안 넘는다**
- [ ] 좁히면 두 칸·한 칸으로 접히고, 접혀도 **어느 트랙인지 알 수 있다**
- [ ] 칩을 다른 단계로 끌면 단계가, **단계 사이**에 놓으면 새 단계가 생긴다(04 유지)
- [ ] 칩을 다른 칸으로 끌었을 때 **기능의 트랙이 바뀐다는 것**이 캡틴에게 전달된다
- [ ] 끄는 동안 **놓을 자리가 보인다**
- [ ] 🟢 기능 보기의 동작은 하나도 안 바뀌었고, 칸 나누기를 **두 벌 짓지 않았다**

**④**
- [ ] 🔴 끝까지 스크롤해도 보기 전환과 `next` 가 **화면에 보이고 동작한다**
- [ ] 붙어 있는 줄이 본문을 **안 가리고**, 밑이 **안 비친다**
- [ ] 좁은 창에서 **가로 스크롤이 안 생긴다**
- [ ] 조작 줄이 지금보다 두꺼워지지 않았다

**공통**
- [ ] 🟢 `extra` 가 그대로 동작하고 firstmate 확인 장치가 여전히 깨어난다
- [ ] 🟢 드래그의 네 검사가 그대로다
- [ ] 🟢 `next` · 어긋남 · 단계 · 순위 계산 결과가 안 바뀐다
- [ ] `features` 탭의 동작이 안 바뀌었다
- [ ] 사양에서 의견 요청 대목이 걷히고 **없앤 이유가 남았다**

## 테스트

🔴 **테스트는 `<패키지>/test/` 에 있다**(`src/` 아래가 아니다). 아래 표는 2026-08-11 에
그 폴더들을 직접 확인한 결과다 — 있다고 적힌 것은 실제로 있다.

| 무엇 | 어디 | 지금 있나 |
|---|---|---|
| 어긋남 0 건이면 어긋남 상자가 안 뜬다 | `frontend/test/plan.test.tsx` | 🟢 **있음**(2026-08-11 추가) — 유지 |
| 첫 화면에 놓는 순간 경고가 없다 | 같은 파일 | 🟢 **있음**(2026-08-11 추가) — 유지 |
| 어긋남이 접히지 않고 바로 보인다 | 같은 파일 | 🟢 있음 — 유지 |
| 드래그 → 쓰기 → 재조회로 값이 남는다 | 같은 파일 | 🟢 있음 — 유지 |
| 단계·트랙 묶기, `next` 버튼, 기능 보기 | 같은 파일 | 🟢 있음 — ③ 이 다시 짜도 **결과는 그대로여야 한다** |
| 🔴 조건이 사라진 경고가 스스로 없어진다 | `frontend/test/plan.test.tsx` | 🔴 **없음 — 이 티켓이 첫 커버** |
| 🔴 계획이 밖에서 바뀌어도 낡은 경고가 안 남는다 | 같은 파일 | 🔴 **없음 — 첫 커버** |
| 🔴 닫은 경고가 배치 변화 전까지 닫혀 있다 | 같은 파일 | 🔴 **없음 — 첫 커버** |
| 🔴 같은 단계가 칸들 사이에서 같은 높이에 선다 | 같은 파일 | 🔴 **없음 — 첫 커버**(③ 이 만드는 약속) |
| 🔴 어느 트랙에 그 단계가 없어도 자리가 유지된다 | 같은 파일 | 🔴 **없음 — 첫 커버** |
| 🔴 스크롤해도 조작 줄이 문서에서 안 사라진다 | 같은 파일 | 🔴 **없음 — 첫 커버**(④) |
| 계획 조회 응답에 의견 요청이 안 실린다 | `backend/test/app.test.ts` | 🟡 지금은 **실린다**는 검사가 있다 — ① 이 **뒤집는다** |
| 🔴 INV-2 — 쓰기가 관리대상 트리를 안 바꾼다 | 같은 파일 | 🟢 **있음, 2026-08-11 강화** — 파일 하나가 아니라 **트리 전체**를 대조한다. 🔴 약하게 되돌리지 마라 |
| `extra` 침묵 규약 · `next` · 어긋남 · 네 검사 | `core`/`core-io`/`cli` 각 단위 | 🟢 있음 — **유지** |
| 의견 관련 테스트 | `backend/test/app.test.ts` · `core/src/plan/opinion.test.ts` | 🔴 **①과 함께 지운다** |

🔴 **표를 믿고 쓰되, 시작할 때 한 번은 직접 확인해라.** 이 표는 하루 사이에 두 번 낡았다.

## 캡틴 확인

**어디서:** gootte `plan` 탭.

**무엇을 하나:** 탭을 열고 맨 위를 본다. 계획을 맨 아래까지 내린 뒤 **거기서** 기능 보기로
바꾸고 `next` 를 눌러 본다. 티켓 칩을 잘못된 자리로 끌어 경고를 띄운 뒤 되돌린다.
단계 보기가 세로 칸으로 서 있는지 본다. 창 너비와 높이를 줄였다 늘려 본다.

**무엇이 보여야 맞나:**

1. 🔴 **맨 위에 정작 보셔야 할 것이 오나** — 의견 요청 상자가 자리를 안 잡고 있나.
2. 🔴 **아무 문제가 없을 때 주황색이 하나도 안 보이나.** 그리고 문제를 되돌렸을 때
   경고가 **저절로** 사라지나 — ✕ 를 눌러야만 사라지면 안 고쳐진 것이다.
3. 🔴 **같은 단계가 칸들 사이에서 나란히 서나** — 이게 안 되면 "같은 단계 = 병렬" 이
   화면에서 사라진다. 순서가 맞나가 아니라 **병렬이 병렬로 보이나**가 판정 기준이다.
4. 🔴 **끝까지 내려가도 조작 줄이 손에 닿나** — 맨 위로 올라갈 일이 없나.
5. 어긋남과 드래그 경고를 나란히 띄웠을 때 **어느 쪽이 무엇인지 알 수 있나.**

## 이 티켓이 하지 않는 것

- **머리말 탭 띠(`features`·`plan`) 자체** — 화면 바깥 틀이다. 손대지 않는다.
- **`features` 탭 개편** — 같은 결함의 수리까지만이다.
- **틈틈이·순서밖의 뜻**(티켓 12) — 종류의 의미를 정하는 일은 별건이다.
- **막힘 줄 오독**(티켓 11) — 터미널 쪽이고 이미 따로 간다.
- **planner 가 먼저 말을 거는 통로** — 지금 필요 없다. 필요해지면 그때 새로 쓴다.
- **되돌리기** — 만들지 않는다(캡틴 결정: 이력 없음).
