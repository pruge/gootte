# T07 — FeatureConflict · ConflictBadge 삭제

**Blocked by:** 없음 (T01~T06과 독립)
**Status:** open
**Time:**

## Goal

conflict 관련 계약 필드와 UI 컴포넌트를 삭제한다.

## Why this slice

T02가 `CopyResolver`·`resolveFile`을 삭제하면 `Feature.conflict`는 항상 빈 배열이거나 남는다. 계약 필드와 화면의 배지 컴포넌트를 함께 지워야 코드베이스가 깔끔하다.

## Produces

`FeatureConflict` 스키마 없는 contract. `ConflictBadge` 컴포넌트 없는 frontend.

## Consumers

- `FeatureCard.tsx` — `ConflictBadge` import/사용
- `TicketRow.tsx` — `ConflictBadge` import/사용
- `FeatureTree.tsx` — `ConflictBadge` import/사용
- `CardDialog.tsx` — `ConflictBadge` import/사용

## Touched surfaces

- `contract/src/index.ts` — `FeatureConflict` 스키마 삭제
- `contract/src/index.ts` — `Feature` 스키마에서 `conflict` 필드 제거
- `frontend/src/components/features/ConflictBadge.tsx` — 파일 삭제
- `frontend/src/components/features/FeatureCard.tsx` — ConflictBadge import/사용 제거
- `frontend/src/components/features/TicketRow.tsx` — ConflictBadge import/사용 제거
- `frontend/src/components/features/FeatureTree.tsx` — ConflictBadge import/사용 제거
- `frontend/src/components/plan/CardDialog.tsx` — ConflictBadge import/사용 제거
- conflict 관련 테스트 케이스 삭제

## Explicitly out of scope

- `CopyResolver` 삭제 (T02)
- `resolveFile` 삭제 (T02)

## Locked decisions

- **D3** — conflict 판정 전부 삭제 (spec)

## Evidence anchors

- `contract/src/index.ts:133-139` — `FeatureConflict` 스키마
- `contract/src/index.ts:164` — `Feature.conflict` 필드
- `frontend/src/components/features/ConflictBadge.tsx` — 배지 컴포넌트

## Regression guards

- 🔴 `pnpm verify` 통과
- 🔴 `Feature` 타입에 `conflict` 필드 없음

## Scope

1. `FeatureConflict` 스키마 삭제
2. `Feature.conflict` 필드 제거
3. `ConflictBadge.tsx` 파일 삭제
4. `FeatureCard.tsx`에서 ConflictBadge import/사용 제거
5. `TicketRow.tsx`에서 ConflictBadge import/사용 제거
6. `FeatureTree.tsx`에서 ConflictBadge import/사용 제거
7. `CardDialog.tsx`에서 ConflictBadge import/사용 제거
8. conflict 관련 테스트 케이스 삭제

## Implementation notes

- `Feature.conflict`가 optional이라 제거해도 기존 코드에서 `?.conflict`로 접근하는 부분만 정리하면 됨

## Acceptance criteria

- AC1 — `Feature` 타입에 `conflict` 필드 없음
- AC2 — ConflictBadge 컴포넌트 없음
- AC3 — `tsc --noEmit` + `vitest` 통과

## Verification

`pnpm verify`
