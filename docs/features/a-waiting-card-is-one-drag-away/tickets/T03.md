# T03 — 내리는 길도 열고, 대기 카드도 클릭하면 티켓이 보인다

**Blocked by:** T02

## Goal

두 가지를 연다.

1. **내리는 끌기** — 위 칸(작업 대상)의 카드를 아래 칸(대기)으로 끌어 놓으면 대기로 내려간다.
2. **대기 카드 선택** — 아래 칸의 카드를 클릭하면 **오른쪽 2/3 에 그 기능의 티켓 목록**이 뜬다.
   위 칸 카드를 클릭했을 때와 **똑같이** 동작한다.

캡틴 지시(2026-09-04): *"역으로 작업중인 거를 대기중으로 내릴 수도 있었으면 좋겠거든. 그리고
대기중인 것도 클릭하면은 옆쪽에 티켓 목록이 나왔으면 좋겠어 동일하게."*

## Why this slice

T02 는 **올리는 방향 하나**만 열었고 내리는 길과 대기 카드 선택을 명시적으로 범위 밖에 뒀다.
캡틴이 써 보고 그 둘을 요청했으므로 지금이 그것을 여는 자리다. 둘을 한 장에 묶는 이유는
**같은 두 곳**(`ProcessView.tsx` 의 선택 계산과 끌기 배선)을 건드리기 때문이다 — 갈라 놓으면
두 스레드가 같은 파일에서 충돌한다.

## Produces

- 위 칸 카드의 끌기 + 아래 칸의 놓기 → `waiting` 이동
- 대기 카드 클릭 → 오른쪽 티켓 목록

## Consumers

캡틴.

## Touched surfaces

- `code/web/frontend/src/components/process/ProcessView.tsx` — 선택 계산(150줄 근처)·`onDragEnd`(186줄 근처)
- `code/web/frontend/src/components/process/WaitingList.tsx` — 카드가 클릭도 받게
- `plan/areas.ts` — **읽기만.** `AREA_DROP_ID("waiting")` · `storedArea` · `changesBoard` 재사용

## Explicitly out of scope

- 예약·폐기·완료 칸 — 여전히 `MoveDialog` 의 몫이다.
- 오른쪽 티켓 컬럼 안의 끌어 놓기.
- plan 탭.
- `process/dnd.ts` 죽은 코드 정리 — **별건이다. 여기서 손대지 마라.**

## Locked decisions

- **선택 후보는 `active ∪ waiting` 이다.** 지금은 `features.find(...) ?? features[0]` 로
  작업 대상 안에서만 찾는다(150줄). 두 목록을 합쳐 찾되 **기본값은 지금대로 `active` 의 첫 번째**다 —
  아무것도 안 골랐을 때의 화면이 바뀌면 안 된다.
- 🔴 **선택은 카드가 칸을 옮겨도 따라간다.** 고른 카드를 끌어 내리면 오른쪽 티켓 목록이 그대로
  남아야 한다 — 방금 보던 것이 사라지면 "내가 뭘 잘못 눌렀나" 가 된다.
- 🔴 **대기가 0 이어도 놓을 자리가 있어야 한다.** T01 이 "대기 0 이면 손잡이·상자를 만들지 않는다"
  로 짜 놨는데, 그러면 **첫 카드를 내릴 자리가 없다.** 끄는 중일 때는 그 한 줄 자리가 놓기 대상이
  되게 하든, 끄는 동안만 상자를 내든 — **방법은 맡기되 "내릴 곳이 없는 상태" 를 남기지 마라.**
  T02 가 위 칸에서 같은 문제를 이미 풀었다(빈 작업 대상에도 놓을 수 있다) — 그 방식을 참고해라.
- **대기로 보내는 것은 "행을 지운다" 는 뜻이다** — `storedArea("waiting")` 는 `null` 이다
  (`areas.ts:31`, INV-B1). 새 값을 만들지 말고 그 함수를 그대로 써라.
- **클릭과 끌기를 가르는 기준은 그대로** `PointerSensor { distance: 6 }` 다. 대기 카드는 이제
  클릭도 끌기도 받으므로 이 값이 둘을 가른다 — 바꾸지 마라.

## Evidence anchors

- `process/ProcessView.tsx:150` — `const current = features.find((f) => f.slug === selected) ?? features[0] ?? null`
- `process/ProcessView.tsx:191` — `if (dropTargetArea(String(e.over.id), undefined) !== "active") return;`
- `process/ProcessView.tsx:95` — `ActiveDropZone`(위 칸 놓기). 아래 칸에도 짝이 필요하다
- `plan/areas.ts:31` — `storedArea = (id) => (id === "waiting" ? null : id)`
- `plan/areas.ts:34` — `AREA_DROP_ID`

## Regression guards

- 🔴 **올리는 끌기(T02)가 그대로 동작한다.**
- 위 칸 카드 클릭 → 티켓 목록, 문서 열기, 이동 아이콘이 그대로다.
- 아무 데도 아닌 곳에 놓으면 아무 일도 없다(T02 의 AC3 가 계속 참이다).
- 경계 끌기(T01)가 그대로 동작한다.
- `pnpm verify` green.

## Scope

1. 선택 계산을 두 목록 합집합으로 넓힌다(기본값은 그대로).
2. `WaitingList` 의 카드가 클릭을 받게 하고, 선택된 카드를 위 칸과 같은 결로 강조한다.
3. 위 칸 카드에 `useDraggable` 을 붙인다.
4. 아래 칸을 놓기 대상으로 만든다 — **대기가 0 인 경우를 반드시 포함**해서.
5. `onDragEnd` 를 두 방향으로 가른다(`active` / `waiting`).
6. 🔴 **실제 브라우저에서 양방향으로 끌어 본다** — `chrome-devtools-axi drag`.
   스킬 `gootte-browser` 와 `gootte-dev-server` 를 먼저 읽어라.

## Implementation notes

- `useDraggable` 을 붙인 카드가 여전히 `<button>` 으로 클릭을 받는지 확인해라 — T02 가 대기 카드에서
  `role` 이 덮이지 않게 배선을 갈라 놨다. 위 칸 카드는 이미 `<button>` 이므로 같은 함정을 조심해라.
- `usePlanMove` 의 낙관 갱신은 **동기**여야 한다(`lib/query.ts:206~211`). `await` 를 끼우지 마라.
- 충돌 판정은 T02 가 `pointerWithin` → `rectIntersection` 까지만 두고 `closestCenter` 로 안 내려갔다
  (안 그러면 아무 데나 놓아도 걸린다). **놓을 자리가 둘이 된 지금 그 선택이 여전히 맞는지 확인**하고,
  바꿨다면 왜 바꿨는지 `## Comments` 에 적어라.

## Acceptance criteria

- AC1 — 위 칸 카드를 아래 칸으로 끌어 놓으면 **대기로 내려가고** 화면·서버 판이 같이 바뀐다.
- AC2 — **대기가 0 인 상태에서도** 첫 카드를 내릴 수 있다.
- AC3 — 아래 칸 카드를 클릭하면 오른쪽에 그 기능의 티켓 목록이 뜬다(위 칸과 같은 내용·같은 동작).
- AC4 — 고른 카드를 다른 칸으로 옮겨도 **선택이 유지된다**(오른쪽 목록이 그대로 남는다).
- AC5 — 올리는 끌기(T02)와 아무 데도 아닌 곳 놓기(T02 AC3)가 여전히 그대로다.
- AC6 — `pnpm verify` green.
- AC7 — 🔴 **실제로 끌어 본 기록**(양방향 각각의 좌표·전후 판)을 `## Comments` 에 남긴다.

## Verification

```
pnpm verify
```
끌기는 실제 브라우저에서.

## Comments

(append-only)
