---
created: 2026-07-25
status: in_progress
priority: high
kind: single
todos: [013-viz-api]
worktree: viz-api
startedAt: 2026-07-25
related_sprints: [2026-07-25-viz-data-model]
---

# viz-api — backend viz 엔드포인트 (T2)
> 단독. 1 worktree = 1 sprint. 012 projection을 서빙 · 014~017 프론트 뷰의 dep 루트.

## scope
- 013-viz-api (high) — `/api/{board,timeline,worktree}/:slug` + `/api/lineage`에 nodes 추가.

## 🔴 Invariant 점검
- **INV-4** — backend는 CORE projection(buildKanban/buildGantt) 릴레이(서버 LLM 0). envelope zod 검증 후 verbatim.
- **INV-2** — read-only(loadProjectState). write 없음.
- **INV-3** — 요청 시 재계산. discover는 캐시(기존).

## 작업 path (예상 phase)
### Phase 1 — 라우트 추가 (app.ts)
- `GET /api/board/:slug` → `BoardResponse` (buildKanban(state, gitSignals)).
- `GET /api/timeline/:slug` → `TimelineResponse` (buildGantt(state) → {rows, from, to}).
- `GET /api/worktree/:slug` → `WorktreeResponse` (state.sprints·worktrees·gitSignals 조립 → WorktreeStatus[]).
- `GET /api/lineage/:slug` → `LineageResponse`에 **nodes 추가**(state.lineage.nodes).
- slug 해소·404·zValidator = 기존 패턴 재사용.

### Phase 2 — WorktreeStatus 조립
- loadProjectState의 worktree↔initiative 매핑 + GitSignal + sprint 링크로 `WorktreeStatus` 구성. (활성 worktree 0이면 빈 배열.)

## 다음 단계 결정 필요
- 없음(spec·012가 닫음).

## 완료 기준
- 013 완료: `app.request` vitest — `/api/{board,timeline,worktree}` envelope zod 검증 + lineage nodes + 404. 픽스처(alpha) + jinwooauto 스모크(board 15·timeline 47행).
- 전체 회귀: `pnpm verify`(54+) green.

## 사용자 테스트
> backend viz 엔드포인트 — 가시 UI는 014~017 뷰에서. 지금은 API curl로 확인 가능. (자동 게이트 `pnpm verify` 58/58 — 제가 머지 전 실행.)

🌐 backend dev (user-runs, 선택)
```
GOOTTE_ROOTS=$HOME/Documents/ai PORT=8788 pnpm -C code/web/backend start
curl -s localhost:8788/api/board/jinwooauto    | jq '.columns|map({(.key):(.items|length)})'   # ready 15
curl -s localhost:8788/api/timeline/jinwooauto | jq '{rows:(.rows|length),from,to}'             # 47행 07-05~07-24
curl -s localhost:8788/api/worktree/jinwooauto | jq '.worktrees|length'                          # 0 (활성 없음)
curl -s localhost:8788/api/lineage/jinwooauto  | jq '{nodes:(.nodes|length),edges:(.edges|length)}'
```

## 관련 todo / spec
- [013-viz-api](../todo/013-viz-api.md) — backend viz 엔드포인트 (T2)
- [spec](../roadmap/project-manager/web-viz/spec.md) · [ADR-0003](../roadmap/project-manager/web-viz/adr/0003-core-projection-boundary.md) · [M-0003](../mermaid/INDEX.md#M-0003)
