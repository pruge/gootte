---
created: 2026-07-25
status: in_progress
priority: normal
kind: single
todos: [011-theme-e2e]
worktree: theme-e2e
startedAt: 2026-07-25
related_sprints: [2026-07-25-plan-lineage-views]
---

# theme-e2e — 디자인 polish + Playwright e2e (T6·T7)
> 단독. 2a 마감. theme 토글/localStorage 는 009 에서 이미 구현 → 여기선 **polish + e2e**.

## scope
- 011-theme-e2e (normal) — 디자인 polish(T6, 두 테마 anti-template 마감) + e2e(T7, dev 서버 부팅→렌더 확인).

## 🔴 Invariant 점검
- 없음(디자인/테스트 — SoT·read-path 무영향). e2e 는 렌더가 서버 데이터와 일치하는지 확인(INV-4 회귀 방어).

## 작업 path (예상 phase)
### Phase 1 — 디자인 polish (T6)
- 두 테마 계층·밀도·리듬 점검: **dark=미션컨트롤**(밀도·단일 accent·monospace ref), **light=에디토리얼**(고대비 타이포·정독). CSS 토큰(간격/타이포 scale) 보강.
- hover/focus/active 상태 의도적 확인 · focus-visible outline 일관 · reduced-motion 존중.
- 슬롭 방지 체크(기본 카드 그리드/균일 padding 회피). Tabler 전용·Pretendard 유지.
- (토글/localStorage/data-theme 는 009 완료 — 회귀만 확인.)

### Phase 2 — Playwright e2e (T7)
- `@playwright/test` 추가 + `playwright.config.ts`(webServer 로 `pnpm dev` 부팅, `GOOTTE_ROOTS` = **backend 픽스처**(`code/web/backend/test/fixtures/roots`) → 결정적·머신 비종속).
- e2e 여정: `/` 로드 → 사이드바 목록 → 프로젝트 선택(URL `?p=`) → plan 탭 렌더 확인 → lineage 탭 전환(URL `?tab=`) → 테마 토글(data-theme 변화).
- 루트 `pnpm e2e` ops 추가(`/cling:ops` 또는 직접).

## 다음 단계 결정 필요
- e2e 대상 = **backend 픽스처 프로젝트**(alpha, 결정적). jinwooauto 실렌더는 사람 eye-check(측정 아님).

## 완료 기준
- 011 완료:
  - **측정**: `pnpm verify` green(회귀) + `pnpm e2e` green(픽스처로 목록→plan→lineage→theme 여정).
  - **사람 eye-check**: `GOOTTE_ROOTS=$HOME/Documents/ai pnpm dev` → jinwooauto 로 두 테마 다 슬롭 아님(계층·밀도·가독) 육안 확인.
- 전체 회귀: 기존 47 vitest green 유지.

## 사용자 테스트
> sprint theme-e2e 완료 기준 — worktree 안 검토용. (자동 게이트: `pnpm verify` 47/47 · `pnpm e2e` 1 passed — 제가 머지 전 실행.)

🧪 e2e (자동, 결정적 — backend 픽스처 alpha)
```
pnpm -C code/web/frontend exec playwright install chromium   # 최초 1회(브라우저)
pnpm e2e   # 목록→alpha 선택→plan→lineage 탭→테마 토글 여정
```

🌐 디자인 eye-check (jinwooauto 실렌더, 사람 판단)
```
GOOTTE_ROOTS=$HOME/Documents/ai pnpm dev   → localhost:5173
```
✅ 확인
- **다크(미션컨트롤)** — 밀도·단일 accent·monospace ref, NOW 강조 · **라이트(에디토리얼)** — 고대비 타이포·정독. 둘 다 슬롭 아님(계층·리듬 의도적)
- focus 이동 시 outline 일관 · selection 색 · 얇은 스크롤바 · 텍스트 selection/hover 상태
- plan/lineage 가독(체인·drop verbatim이 라이트에서 읽기 좋은지)

## 관련 todo / spec
- [011-theme-e2e](../todo/011-theme-e2e.md) — theme polish + e2e (T6·T7)
- [spec](../roadmap/project-manager/web-dashboard/spec.md) · [ADR-0004 theme](../roadmap/project-manager/web-dashboard/adr/0004-theme-design-direction.md) · [M-0002](../mermaid/INDEX.md#M-0002)
