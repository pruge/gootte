# codegraph 쓰는 법 (gootte)

색인은 **코드만 안다.** `import`·`function`·`interface`·`constant`·`type_alias` 같은 영문 심볼 노드뿐이라
한국어로 물으면 걸린다는 보장이 없다.
티켓의 한국어 개념어("실시간 저장소", "프로젝트 발견")를 codegraph 가 아는 영문 심볼로 먼저 바꿔야 하고,
그 변환표가 [`vocabulary/`](vocabulary/) 다.

사전은 이 저장소의 여섯 컨텍스트(`code/web/` 아래 워크스페이스)대로 쪼개져 있다:
`contract.md` `core.md` `core-io.md` `cli.md` `backend.md` `frontend.md`.
🔴 **어느 파일에 있을지 추측하지 마라** — 그걸 알면 이미 답을 아는 것이다.
찾을 때는 항상 `vocabulary/` **디렉토리 전체를 grep** 한다.

## 이 저장소의 색인 위치

색인은 **저장소 뿌리**의 `.codegraph/` 에 있다(gitignore, 머신 로컬).
자매 저장소 jinwooauto 와 같다. **명령에 경로를 줄 필요가 없다** — 뿌리에서 그냥 쓴다.

```
codegraph status
```

격리 작업 사본에는 색인이 아예 없을 수 있다. 없다고 나오면 **저장소 뿌리에서**
`codegraph init` 으로 만든다.

🔴 **`init code/web` 처럼 하위 경로에 만들지 마라.** 뿌리 색인이 이미 `code/web/` 아래를
전부 담고 있어서, 하위에 하나 더 만들면 **색인이 둘로 갈라지고** 좁은 쪽이 뿌리 쪽 답을
못 낸다. 명령에 하위 경로를 줘도 codegraph 는 **위로 올라가며 가장 가까운 `.codegraph/` 를**
찾으므로, 색인이 뿌리 하나뿐이면 경로를 줘도 같은 답이 나온다 — 그래서 경로는 붙일 이유가 없고,
`init` 만이 진짜 해를 끼친다.

🔴 **색인 없이 질의하면 전부 "없음" 으로 나온다** — 그 결과를 사전에 적으면
이 사전이 오판을 기록하는 장치가 된다.

> **2026-08-10 정정.** 이 절은 첫 커밋(#1·#2)부터 색인이 `code/web/.codegraph/` 에 있다고 적어
> 왔고, 그 문장이 그대로 옮겨 다녔다. **그런 자리는 존재한 적이 없다** — 캡틴 사본에서 직접 확인했다.
> 다행히 경로를 줘도 위로 올라가 뿌리 색인을 찾았기에 답이 틀리지는 않았다. **`init code/web`
> 한 줄만이 실제 위험이었다.**

## 순서

1. 개념어로 사전 전체를 grep 한다. 걸리면 영문 앵커로 바로 질의한다.

   ```
   grep -rn "실시간" docs/agents/codegraph/vocabulary/
   ```

2. 없으면 `codegraph query "<짐작한 영문 이름>"` 으로 시도한다.
   **영문 식별자로 질의해야 걸린다** — 자연어 한국어 질문은 앵커 추출이 불안정하다.
3. 결과가 나오면 그 앵커로 필요한 명령을 쓴다.
4. `No results found` 는 **"코드에 없다" 가 아니다.** 아래 §없다고 말하기 전에 를 밟아라.
5. 찾느라 헤맸다면 사전에 한 줄 남긴다 — 아래 §사전에 한 줄 남기기.

## 명령 고르기

🔴 **`explore` 하나로 버티지 마라.** 명령은 열 개고, 질문이 정해져 있으면 그 질문의 명령이 따로 있다 —
특히 **고치기 전 `impact`**(무엇이 깨지나)와 **고친 뒤 `affected`**(무슨 테스트를 돌리나)다.
어느 명령이 어느 질문에 답하는지와 출력 읽는 법은 [`AGENTS.md`](../../../AGENTS.md) §구조 파악이
소유하고, 문법은 `codegraph --help` 가 소유한다. 이 파일은 **사전**만 소유한다.

## 없다고 말하기 전에

`codegraph status` 의 "up to date" 를 **신선도 근거로 쓰지 마라** — 색인은 **부분적으로** 낡을 수 있고,
그 상태에서도 그렇게 답한다.

없다고 판단하기 전에 반드시 이 순서를 밟는다:

1. `grep -rn "<이름>" code/` 또는 파일을 직접 읽어 **교차확인한다.**
2. 그래도 없으면 `codegraph index` 로 **재색인한 뒤 다시 묻는다.**
3. 그 다음에야 "없다" 고 말할 수 있다.

건너뛰면 **이미 있는 것을 없는 줄 알고 다시 구현하게 된다.**

## 사전에 한 줄 남기기 (크루)

**언제.** 한국어 개념어에서 코드를 찾느라 **헛짚었을 때만.**
헛짚었다는 것 자체가 그 대응이 비자명하다는 증거고, 다음 크루도 같은 데서 막힌다는 뜻이다.
한 번에 맞췄으면 적지 마라. 보통 작업당 0~2줄이다.

**무엇.** 심볼 이름과 **파일 경로만** 적는다.
🔴 **줄번호는 적지 마라** — 가장 빨리 썩는 값이고, 줄번호가 밀린 사전은 없는 것보다 나쁘다.
심볼 이름과 파일 경로는 PR 몇 개가 착지해도 견딘다.
줄은 필요할 때 `codegraph node "<심볼>"` 으로 즉시 나온다.

**어떻게.**

1. `codegraph query "<앵커>"` 로 결과가 나오는지 확인한다.
   안 나오면 위 §없다고 말하기 전에 를 밟는다. 그래도 없으면 적지 않는다.
2. **경로가 파일을 정한다.** `code/web/backend/...` 면 `vocabulary/backend.md`,
   `code/web/contract/...` 면 `vocabulary/contract.md`. 해당 파일이 없으면 새로 만든다
   (첫 줄에 `# <컨텍스트> — 질의어` 와 그 컨텍스트의 코드 경로를 적고 같은 표 헤더를 쓴다).
   그 표 맨 아래에 한 줄 추가한다:

   ```
   | <한국어 개념어> | `<영문 앵커>` | <종류> | `<파일 경로>` | <YYYY-MM-DD> | <비고 또는 빈칸> |
   ```

   **한 심볼이 두 컨텍스트에 있으면** 형(type)을 소유한 쪽에 적고, 비고에서 다른 구현을 가리킨다.
   양쪽에 같은 줄을 복사하지 마라 — 둘이 따로 썩는다.

3. 🔴 **기능 커밋과 분리해 단독 커밋으로 만든다:**

   ```
   docs(codegraph): add "<개념어>" → <영문 앵커>
   ```

   기능 커밋에 섞지 마라. 섞으면 리뷰에서 묻히고, **그 PR 이 엎어질 때 검증된 지식까지 같이 사라진다.**
   단독 커밋이면 리뷰어가 그 한 줄만 따로 살릴 수 있다.

4. 그대로 자기 PR 에 실어 보낸다. 별도 절차 없다 — 머지되면 사전에 들어간 것이다.

**충돌.** 컨텍스트가 갈리면 대개 충돌하지 않는다.
같은 컨텍스트 파일에 두 브랜치가 동시에 추가해 충돌하면 해결은 항상 **둘 다 살리기**다 —
한쪽을 버리지 마라. 각 줄은 서로 독립적이다.

**같은 개념어가 이미 있으면.** 새 줄을 만들지 말고 기존 줄을 고친다.
심볼이 바뀐 경우라면 앵커·경로·확인일을 갱신하고,
커밋 메시지는 `docs(codegraph): update "<개념어>" → <새 앵커>` 로 한다.

## 왜 이렇게 만들었나 / 유지보수 기준

운영 계획·서식 기준·이 사전이 만들어진 배경은 [`PLAN.md`](PLAN.md) 참고.
크루는 이 파일과 `vocabulary/` 만 보면 되고 `PLAN.md` 는 몰라도 된다.
