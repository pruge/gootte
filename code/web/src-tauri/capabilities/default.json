{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "gootte ui is plain http against the local stack — the only ipc it uses is the settings folder dialog (tauri-desktop-app T02).",
  "windows": ["main"],
  "permissions": ["dialog:allow-open"]
}
