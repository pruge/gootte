[package]
name = "gootte-desktop"
version = "0.1.0"
description = "gootte desktop shell — wraps the existing web ui in a native window"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
libc = "0.2"
tauri = { version = "2", features = [] }
